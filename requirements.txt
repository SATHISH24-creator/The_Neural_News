# Streamlit and related packages
streamlit==1.17.0  # Ensure this version is what you're using or adjust accordingly

# Google Sheets integration
gspread==5.4.0
oauth2client==4.1.3
google-auth==2.6.0
google-auth-oauthlib==0.4.6
google-auth-httplib2==0.1.0

# Data parsing and handling
feedparser==6.0.8
python-dateutil==2.8.2
requests==2.28.1
beautifulsoup4==4.11.1  # Updated to the correct BeautifulSoup version
pyyaml==6.0  # Often needed for Streamlit deployments

# Time and date utilities
time  # This is a standard Python module, no need to include it here

# Document and Data handling
re  # Regular expressions, standard Python module, no need to include
datetime  # Standard Python module, no need to include

# Google credentials for ServiceAccount
oauth2client==4.1.3  # Already included, if needed for backwards compatibility

# Other packages
io  # In case you're using BytesIO, it's part of the `io` standard module
